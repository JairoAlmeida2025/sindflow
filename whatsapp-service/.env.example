# WhatsApp Service (Baileys) - Environment Variables

# HTTP server port
PORT=4000

# Allow cross-origin calls from your frontend domain
# Example: https://sindflow.vercel.app
CORS_ORIGIN=https://sindflow.vercel.app

# Sessions directory (persistent)
# In Easy Panel, mount a volume to this path
# Example: /app/whatsapp-service/sessions
WHATSAPP_SESSIONS_DIR=/app/whatsapp-service/sessions

# (Optional) JWT secret if you want to validate Authorization headers later
JWT_SECRET=change_me_strong_secret

# (Optional) Public base URL if needed for logs
PUBLIC_URL=https://whatsapp.yourdomain.com
